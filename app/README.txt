Steps to run the application:
1. Open Anaconda Command Promp
2. Navigate to the folder where you extracted the "Application.rar" files
3. Type "streamlit run app.py"
4. A localhost browser window having the application interface will open